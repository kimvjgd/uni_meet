name: uni_meet
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.16.1 <3.0.0"

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter


  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.2
  firebase_auth: ^3.1.3
  firebase_core: ^1.12.0
  fluttertoast: ^8.0.8
  cloud_firestore: ^3.1.10
  get: ^4.6.1
  image_picker: ^0.8.4+4
  path_provider: ^2.0.8
  firebase_storage: ^10.2.9
  http: ^0.13.4
  group_radio_button: ^1.2.0
  flutter_typeahead: ^3.1.3
  confetti: ^0.6.0
  logger: ^1.1.0
  timeago: ^3.2.2
  firebase_messaging: ^11.2.4
  flutter_local_notifications: ^9.4.0
  settings_ui: ^2.0.2
  url_launcher: ^6.0.20
  extended_image: ^6.0.2+1


dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^1.0.0

flutter:

  uses-material-design: true

  assets:
    - assets/images/uniexample.png
    - assets/images/diamond.png
    - assets/images/game_banner.png
    - assets/images/game_title.png
    - assets/images/momo1.png
    - assets/images/momo2.png
    - assets/images/momo3.png
    - assets/images/momo4.png
    - assets/images/momo5.png
  #   - images/a_dot_ham.jpeg
