import 'package:flutter/material.dart';

class InfoFieldPage extends StatelessWidget {
  const InfoFieldPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
